services:
  web:
    build:
      context: ./website/
      target: dev
    ports:
      - "5000:5000"
    depends_on:
      - server
    volumes:
      - ./website/app:/app/app
      - ./website/components:/app/components
      - ./website/utils:/app/utils

  server:
    build: 
      context: .
      target: dev
    ports:
      - "5001:5001"
    volumes:
      - ./db/data.db:/code/db/data.db:rw
      - ./db/auth.db:/code/db/auth.db:rw
      - ./assets/thumbnails:/code/assets/thumbnails/
      - ./engine:/code/engine/
      - ./internal:/code/internal/